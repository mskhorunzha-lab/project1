DATABASE_URL="postgresql://infra:infra@localhost:5432/infra_portal?schema=public"

# Optional for local/demo API calls. In production requests must provide a
# valid infra_user_id cookie or x-user-id / x-user-email header.
INFRA_PORTAL_DEFAULT_USER_EMAIL="admin@corp.local"
